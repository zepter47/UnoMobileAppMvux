<Page
    x:Class="EstatesUpdated.View.ResidentsListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EstatesUpdated.View"
    xmlns:ut="using:Uno.Themes"
    xmlns:utu="using:Uno.Toolkit.UI"
    xmlns:vvm="using:EstatesUpdated.Vm"
    xmlns:uen="using:Uno.Extensions.Navigation.UI"
    xmlns:mvux="using:Uno.Extensions.Reactive.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vvm:ResidentsListViewModel/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Residents in the Estate" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center"
                   Margin="8"/>

        <TextBox Width="150" HorizontalAlignment="Left" Grid.Row="1" Margin="8" Height="2" PlaceholderText="Search">
            <!--ut:ControlExtensions.Icon>
                    <PathIcon Data="{StaticResource Search}"/>
                </ut:ControlExtensions.Icon-->
        </TextBox>

        <Button Content="Add Resident" uen:Navigation.Request="AddResidents" Grid.Row="1"
                    HorizontalAlignment="Right" Margin="8" Width="180">
            <ut:ControlExtensions.Icon>
                <PathIcon Data="{StaticResource Add}" />
            </ut:ControlExtensions.Icon>
        </Button>

        <!--Button Content="Views" HorizontalAlignment="Center" Grid.Row="1" Command="{Binding RetrieveTenantDetailsCommand}"/-->

        <mvux:FeedView Source="{Binding TenantsList}" Grid.Row="3">

            <DataTemplate>
                <ListView ItemsSource="{Binding Data}"  Margin="9 2 5 0" uen:Navigation.Request="ViewResidents">

                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>

                    <ListView.ItemTemplate>
                        <DataTemplate>

                            <Grid Margin="10 5 5 0" BorderBrush="HotPink" BorderThickness=".3" CornerRadius="5"
                          Background="{ThemeResource OnSecondaryColor}" Padding="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal" Spacing="5">
                                    <TextBlock Text="{Binding FirstName}" FontWeight="SemiBold" FontSize="18"/>
                                    <TextBlock Text="{Binding LastName}"  FontWeight="SemiBold" FontSize="18"/>
                                </StackPanel>
                                <TextBlock Text="{Binding TdHouse}"  FontWeight="SemiBold" FontSize="18" Grid.Column="1"
                                   Grid.Row="0" HorizontalAlignment="Right"/>

                            </Grid>

                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>

            </DataTemplate>

            <mvux:FeedView.ProgressTemplate>
                <DataTemplate>
                    <ProgressBar/>
                </DataTemplate>
            </mvux:FeedView.ProgressTemplate>

            <!--mvux:FeedView.ErrorTemplate>
                <DataTemplate>
                    <TextBlock Text="Error has occured during Loading"/>
                </DataTemplate>
            </mvux:FeedView.ErrorTemplate>

            <mvux:FeedView.NoneTemplate>
                <DataTemplate>
                    <TextBlock Text="No Results"/>
                </DataTemplate>
            </mvux:FeedView.NoneTemplate-->
            
        </mvux:FeedView>

    </Grid>
</Page>
